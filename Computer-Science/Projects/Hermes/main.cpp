#include <iostream>
#include <stdlib.h>
#include <fstream>

class classGenerator
{
private:
    bool isValid(unsigned int x, unsigned int y, unsigned int subjectNum)
    {
        return true;
    }
    const char* students;
    int noOfstudents = 0;
public:
    //bool intakeLesson(unsigned int lenX, unsigned int lenY, unsigned int subjectNum, unsigned int studentNum, unsigned int* studentNums)
    //{
    //    return true;
    //}
    int intakeStudent(const char* name, int* lessons)
    {
        
    }
};